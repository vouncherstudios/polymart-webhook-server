{
  "name": "polymart-webhook-server",
  "version": "1.1.1",
  "description": "A lightweight server to receive and validate Polymart webhooks, and forward the formatted information to a Discord channel.",
  "scripts": {
    "start": "pip3 install -U -r .requirements.txt -t .data/site-packages && env PYTHONPATH=$PWD/.data/site-packages python3 server.py"
  },
  "dependencies": {},
  "engines": {
    "node": "16.x"
  },
  "license": "MIT",
  "keywords": [
    "node",
    "glitch",
    "polymart",
    "discord"
  ]
}